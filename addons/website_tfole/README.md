# website_tfole

The single Odoo module behind the Tfole storefront. Deliberately **not** a
`theme_*` module — see CLAUDE.md for why.

Empty at Phase 0: the stack exists, the module does not. The manifest, asset
bundles and base data arrive in Phase 1.
