# data/

Seed data and product imports, version-controlled so that rebuilding a clean
database is a one-command operation.

Nothing here yet — the product CSVs land in Phase 1 (categories first, then
products, then variants and images).
