# tests/

Playwright specs. Added in Phase 9; `make test` is a no-op until then.

Planned coverage: shop loads, product page loads, add to cart, full checkout
with cash on delivery, language switch, and an RTL layout snapshot.
